{
    "buildCommand": "prisma generate && next build --webpack",
    "installCommand": "npm install",
    "framework": "nextjs",
    "regions": [
        "arn1"
    ],
    "crons": [
        {
            "path": "/api/cron/webhooks",
            "schedule": "*/10 * * * *"
        }
    ]
}